[project]
name = "ddup"
version = "0.1.0"
description = "A-share quantitative strategy backtesting engine"
license = {text = "MIT"}
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.0",
    "numpy>=2.0",
    "numexpr>=2.14.2",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.5",
    "pyarrow>=15.0",
    # ML 训练/推理/导出（test_ml / test_ml_train 依赖，缺包时仅相关用例跳过）
    "onnxruntime>=1.17",
    "xgboost>=2.0",
    "onnxmltools>=1.11",
    "scikit-learn>=1.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["btcore", "adapters", "strategies", "factors", "research", "scripts"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.ruff.format]
quote-style = "double"



[tool.pytest.ini_options]
markers = [
    "real_db: tests requiring a real market database connection",
]
