# ddup 整改指导（2026-08-06 全面代码审查）

审查范围：全仓 6 路并行侦查（引擎/撮合、因子子系统、数据层、ML 子系统、研究+CLI、测试+文档+卫生）+ 主线逐条复核。
基线：`ruff` 全绿；`check_anticorrupt.py` 通过；`check_skill_sync.py` 6 个 skill 一致；`pytest` **539 passed**（3 个 RuntimeWarning，见 TEST-07）。

上一轮（2026-08-04）架构审计的排毒项已全部落地（labels run_id 过滤、costs 单工厂、Engine 公共 replay 面、compute_breadth 归并+伪列附着、bool 加法算术化、round_trip 同日分红优先等），本轮不重复；仅遗留决策项跨轮保留（见 §8）。

**总体判断：无 P0 级新缺陷（无静默错结果/契约破坏）。** 核心架构保持健康。优化面集中在三类：①性能热路径（因子物化、统计、撮合、DB 写入）；②口径/实现不一致（三套 realized pnl、双份读取器、双份特征行组装等）；③文档与模板漂移（量大但全部机械可修）。以下每条均给出位置、问题、建议、验证方式；`[√]` = 主线已核实行号，`[侦查]` = 侦查报告所报、行号需复核。

---

## 1. P1 性能 / 规模

### PERF-01 neutralize 逐日 Python OLS 是全库最重算子 `[√]`
- **位置**：`btcore/factors/ops.py:152-166`（`_xs_neutralize` → `_resid`）
- **问题**：按日 `groupby(...).apply(_resid)`，每一天内部重新 `pd.get_dummies` + `concat` + `lstsq`。Python 级逐日循环 + 每日哑变量帧分配，全库最慢算子（mom5_neutral 等 5 个中性化因子都在用）。
- **建议**：一次性为全面板构造哑变量（或缓存按日的 dummies），按 `(date, g)` 分组做向量化 OLS；`ok.sum() <= mat.shape[1]` 门槛语义保持。
- **验证**：`bench_universe_preload.py` 加 neutralize 因子前后对比；`pytest tests/test_factor_ops.py` 中性化用例数值不变。

### PERF-02 symbol_contribution 二次方过滤 `[√]`
- **位置**：`btcore/stats.py:413-414`
- **问题**：每 symbol 组内 `div_log[div_log["symbol"] == sym]` 全表布尔扫描，O(S×D)；3000 标的 × 2 万分红行 ≈ 6000 万次比较/每次统计。
- **建议**：提前一次性 `div_log.groupby("symbol")["net_amount"].sum()` 查表。
- **验证**：`pytest tests/test_cross_validate.py` / stats 相关测试数值不变。

### PERF-03 build_guard_samples 逐行 Python 循环 `[侦查]`
- **位置**：`btcore/ml/labels.py:220-252`
- **问题**：`pos_bars.iloc[i]` + `day.to_dict()` + 每特征 `dict.get` 逐行组装，O(行×特征)。holding 大样本（数千回合×数百日）分钟级。
- **建议**：`pos_bars` 列转 numpy 数组，state 特征与 label 掩码全向量计算。
- **验证**：`pytest tests/test_ml.py::TestGuardSamples`；对拍逐行版与向量版输出 diff=0。

### PERF-04 exit_conditions 对空条件持仓也白算涨跌停 `[√]`
- **位置**：`btcore/match/conditions.py:91-95`
- **问题**：对每个未锁定且有 bar 的持仓，无条件先调 `limits_fn`（Decimal quantize，相对昂贵），即使 `holding.conditions` 为空或条件全部不触发。
- **建议**：`if not holding.conditions: continue` 前置；`_, down = limits_fn(...)` 与 down 检查移入条件触发路径（`executed` 为真之后才需要）。
- **验证**：`pytest tests/test_conditions.py`；实测 step 耗时。

### PERF-05 同日同 symbol 涨跌停价重复计算 `[侦查]`
- **位置**：`btcore/match/manual.py:48,213,263` + `btcore/match/conditions.py:93,216`
- **问题**：manual_sell 部分卖出后 exit_conditions 同 symbol 再算一次；rebalance 卖出/买入两阶段各算一次。
- **建议**：step 级按 `(symbol, trade_date)` memo `limits_fn` 结果（确定性纯函数）。
- **验证**：撮合逐笔测试（V3 影子重算类）不变。

### PERF-06 write_holdings 每日全量重写 `[√]`
- **位置**：`btcore/database.py:138-153`，`btcore/engine.py:642` 每日调用
- **问题**：每日 `DELETE FROM holdings` + 全量重插；该表回测运行期无任何读方（btcore/research/scripts 无 SELECT）。每日一次写放大，且与 `_settle` 里逐行 `write_trade` 叠加。
- **建议**：改为持仓变化时才写，或 run 末尾写一次终态快照（保留 schema 供外部工具读取）。⚠️ 行为变更：若外部脚本依赖逐日 holdings 快照，需先确认无消费方。
- **验证**：`pytest tests/test_invariants/` + 一次真实回测后检查 db 内容。

### PERF-07 _bench_cache 永不命中 `[√]`
- **位置**：`btcore/provider.py:51-57,151-154`
- **问题**：缓存键 `(code, lookback_start, prev)` 随滑窗每日变化，除"同区间重复调用"外命中率≈0，但每窗一帧原始 frame 累积（10 年回测 ≈5000 帧）。注释自认"刻意不清理"。
- **建议**：缓存 `pct_change` 后的 returns Series（或 LRU 上限）；单标的索引查询本身便宜，也可直接删缓存。
- **验证**：`get_benchmark_trend` 单测 + 长回测内存观察。

### PERF-08 get_dividends_on_date 首次调用全表加载 `[√]`
- **位置**：`btcore/generic_sql.py:193-218`
- **问题**：首次调用把整张 dividend 表（无日期上界）全量拉入内存建嵌套 dict；每个 CLI 进程重来一遍（回测首日卡顿 + 内存尖峰）。
- **建议**：按回测窗口 `[min,max] ex_date` 剪枝（provider 传入区间），或进程内持久化 `_div_idx` 复用。
- **验证**：真实库跑 run，观察首日耗时与 RSS。

### PERF-09 物化面板全 float64、无 downcast `[√grep]`
- **位置**：`btcore/factors/plan.py:291-315`；btcore 全库无 float32 路径
- **问题**：5000×2000 面板 × N 因子内存线性膨胀。
- **建议**：物化后按列 downcast float32（先对 neutralize/corr 数值影响做验证）；可选，收益取决于面板规模。
- **验证**：`bench_universe_preload.py` 前后对比。

### 其他 P1 候选（侦查报告，行号需复核）
- `btcore/factors/ops.py:86-92` `_roll_cov/_roll_var` 闭式矩：一次 corr/beta/resid_std 求值 = 5~7 遍 groupby.rolling（corr 单独 7 遍），且大量级序列存在抵消精度风险。建议三矩（mean_x/mean_y/mean_xy）一次算齐共享。
- `btcore/factors/ops.py:123-133` zscore 两次 transform（std 内部再算 mean）共 3 遍扫面板；winsorize 每日 2 次 quantile transform。改 `g.agg` 一次取 mean/std。
- `research/factor_eval.py` calc_ic 双 groupby.apply 为滚动 IC 计算热点。
- `research/attribution.py:52,58-61` 逐日 `.loc[(date, sym)]` 循环 + 重复 ffill；且手写 `ro&immutable` 裸 SQL 绕过 TushareBackend 表单声明（表改列名此处静默漂移）——建议复用 GenericSQLBackend 或抽公共只读连接工厂。
- `research/composite.py` 逐列权重广播内存 churn。
- `btcore/engine.py:647-648` `_settle` 逐 trade 单行 INSERT（对照 `write_ml_predictions` 已是 executemany）——攒批一次提交。
- `btcore/stats.py:211-220` round_trip FIFO 用 iterrows 逐行建事件 + 每 trip 两次 `pd.Timestamp` 字符串解析；10 万+ 行 trade_log 时是 stats 主耗时。

---

## 2. P2 口径 / 一致性

### CONS-01 三套 realized pnl 口径并存 `[√]`
- **位置**：`btcore/stats.py:284-289`（round_trip：`matched*sell_price - buy_cost + div`，**不含费用**）vs `btcore/stats.py:413-415`（symbol_contribution：net_amount，**含费用**）vs `btcore/ml/labels.py:113`（FIFO `pnl += net_amount`，含费用）
- **问题**：同一"往返盈亏"概念三种定义，stats 内部键之间已不一致；ML 训练标签又是一套。报告与交叉验证数字可比性受损。
- **建议**：统一定义（建议统一为含费用净额，与账户恒等式一致），或至少在键名/文档标注口径差异。改动会改变 round_trip 系列指标数字——需要一次全量重跑基线并记录。
- **验证**：stats 测试 + 对同一结果库重算 diff；改后同步 docs 指标说明。

### CONS-02 双份结果读取器 + trade_log 排序漂移 `[√]`
- **位置**：`btcore/engine.py:396-403`（内联 SELECT，`ORDER BY id`）vs `btcore/database.py:206-218`（`read_run_data`，`ORDER BY date, id`）
- **问题**：run() 内联复刻两条 SELECT，且 trade_log 排序不同——实盘回放乱序写入时在线/离线统计事件序漂移；未来 schema 变更两处不同步。
- **建议**：run() 改调 `read_run_data`，统一读取器与排序。
- **验证**：`pytest tests/test_debug_snapshot.py` + 一次回测 stats 与 replay 重算 diff=0。

### CONS-03 total_dividend_received 不含未平仓分红 `[√]`
- **位置**：`btcore/stats.py:355`
- **问题**：`sum(t["dividend_received"] for t in trip_detail)` 只统计已实现 trip；期末仍有持仓时其 accrued 分红不进汇总，分红总额静默低估。
- **建议**：汇总 += open_positions 的 dividend_received，或输出 `total_dividend_accrued` 双口径。
- **验证**：构造期末持仓含分红的 fixture 用例。

### CONS-04 SELL 超出 open lots 静默吞股数 `[√]`
- **位置**：`btcore/stats.py:282-303`
- **问题**：`while remaining > 0 and lots[sym]` 退出后 `remaining` 可能仍 >0，无任何告警，realized pnl 静默少算。
- **建议**：`remaining > 0` 时 `logger.warning`（或按 AGENTS.md fail-fast 原则 raise）。
- **验证**：注入超额卖出 fixture，断言告警/异常。

### CONS-05 where 算子：加载期放行、物化期才崩 `[√]`
- **位置**：`btcore/factors/library.py:54`（docstring "where 只允许纯表达式"）vs `:72-77`（加载期对算子 where 走 `validate_op_expr` 放行）
- **问题**：纯 expr 因子（expr 无算子调用）携带算子 where 时，加载通过；物化期该因子走纯表达式路径，`validate_expr` 拒绝 Call → 运行中才崩。文档与行为也互相矛盾。
- **建议**：加载期统一：纯 expr 因子的 where 必须纯表达式（fail-fast），或全部走 ops 求值器。
- **验证**：构造 `expr: "close / open"` + `where: "delay(x,1)>0"` 用例，断言加载即报错。

### CONS-06 ddof 口径不一致 `[侦查]`
- **位置**：`btcore/factors/ops.py:64-65` vs `:90-104`
- **问题**：`std`/`zscore` 用样本 ddof=1（pandas 默认），corr/beta/resid_std 内部矩 ddof=0；混合表达式差 n/(n−1)，docs §6.1 未注明。
- **建议**：统一口径，或 docs 逐算子声明 ddof。
- **验证**：对拍同序列 std 与 corr 内部矩。

### CONS-07 trainer._scale 与推理侧 inf 处理不对称 `[√]`
- **位置**：`btcore/ml/trainer.py:70`（`np.nan_to_num(..., nan=0.0)`，±inf 保留为 ±1.79e308）vs `btcore/ml/runtime.py:67`（显式 posinf/neginf=0）
- **问题**：训练侧仅靠 dataset.py:99 预清洗 inf 兜底；直接喂未清洗面板训练即发散。
- **建议**：`_scale` 加 `posinf=0.0, neginf=0.0`。
- **验证**：构造含 inf 特征面板跑 train_panel，断言不 NaN 发散。

### CONS-08 ml_train 训练域空交集静默回退 `[√]`
- **位置**：`scripts/ml_train.py:189`：`trade_symbols = sorted(set(trade_symbols) & set(symbols)) or trade_symbols`
- **问题**：index_universe ∩ trade_symbols 为空时静默绕过训练域过滤，延后到 apply_pit_membership 才以误导性错误暴露。
- **建议**：空交集直接 fail-fast 并打印两侧规模。
- **验证**：传一个不相交 universe 参数跑 ml_train，断言报错。

---

## 3. P2 重复代码 / 可维护性

### DUP-01 涨跌停/非法价格 warn+continue 五处逐字重复 `[侦查]`
- **位置**：`btcore/match/manual.py:50-61,123-134,215-226,265-276` + `btcore/match/conditions.py:218-232`
- **问题**：LIMIT_UNKNOWN/INVALID_PRICE/LIMIT_UP|DOWN 三连在 5 个撮合路径重复，仅日志文案微差（如 LIMIT_UNKNOWN 时 buy/sell 文案 up/down 变量名不一致）。
- **建议**：提取统一 `_warn_skip_reason(reason, side, warn, ...)`，消除文案漂移。
- **验证**：撮合路径既有测试全绿 + 文案单测。

### DUP-02 est_price/est_net 现金不足跳过块三处重复 `[侦查]`
- **位置**：`btcore/match/manual.py:148-154,285-291` + `btcore/match/conditions.py:243-250`
- **问题**：5 行"现金不足跳过"逐字重复三处。
- **建议**：提取 `_cash_affordable(account, base_price, shares, slip_fn, costs_fn, slip_ticks=None)`。
- **验证**：buy 路径测试不变。

### DUP-03 _validate_select_actions 是 engine.py 最大函数 `[√结构]`
- **位置**：`btcore/engine.py:76-216`（~140 行，buy_conditions 校验 157-196 占近半）
- **问题**：单函数过大；live 回放/测试无法复用 buy_conditions 校验。
- **建议**：拆出 `_validate_buy_conditions(conds)`。
- **验证**：`pytest tests/test_engine_hardening.py` 全绿。

### DUP-04 CSE 合成节点不重写 `[√]`
- **位置**：`btcore/factors/cse.py:95-111`
- **问题**：`_Rewriter` 只遍历原始 trees，合成 `__cse_N` 节点的 expr 不重写：公共子树"独立出现 + 嵌套在更大被提取子树内"时被算两次；贪心 claim 也阻断部分嵌套候选。
- **建议**：提取后对全表（含合成节点）再跑一轮重写。
- **验证**：`pytest tests/test_factor_cse.py` + 嵌套场景回归用例；物化面板与旧结果 diff 等价。

### DUP-05 CSE 只提取 Call，纯算术重复不共享 `[侦查]`
- **位置**：`btcore/factors/cse.py:53-55`；实例 `factors/library.yaml:416-417`（mf_big_small_div 的 4 项连加块同表达式出现 2 次）
- **问题**：纯算术公共子树重复求值。
- **建议**：候选扩展到无调用 BinOp 子树，或建议作者抽中间因子（更简单）。
- **验证**：因子物化等价 + CSE 计数测试。

### DUP-06 训练/推理特征行组装双份实现 `[侦查]`
- **位置**：`btcore/ml/labels.py:245-251` vs `btcore/ml/runtime.py:162-174`
- **问题**：state 优先→bar 兜底→NaN 语义在训练样本构建与推理侧重复实现（`compute_state_features` 已共享，行组装未共享）。
- **建议**：抽共享 `assemble_row(spec, bar, state)` 供两侧调用。
- **验证**：`pytest tests/test_ml.py` 全绿 + 双份输出对拍。

### DUP-07 scaler 数学+缺失填充三处实现 `[侦查]`
- **位置**：`btcore/ml/runtime.py:59-67` + `btcore/ml/export.py:99-100` + `btcore/ml/trainer.py:68-70`
- **问题**：`export._verify` 注释称"不重复内联第三份"但实际内联了 nan_to_num 第三份。
- **建议**：抽公共 scaler 工具函数（同时解决 CONS-07）。
- **验证**：trainer/runtime 相关测试 + meta verify 测试。

### DUP-08 其余零散重复 `[侦查]`
- `btcore/strategy_tools.py:48-59`：materialize_only 分支与普通分支各自重复 `name not in df.columns` 校验——提到循环顶部统一检查。
- `btcore/strategy_loader.py:219-248` + `btcore/filters.py:104-112`：index_universe 同一 `[start-45, end]` 区间被查两次（universe hook 与 StockFilter）——让 universe hook 结果注入 StockFilter 或复用 provider 缓存。
- `btcore/ml/labels.py:97`：trade_log 7 列名硬编码进 SQL，与 database.py:45-61 canonical schema 双处声明——共享列名常量或 SELECT * + 显式列映射。
- `scripts/replay.py`：唯一未薄壳化的脚本（快照回放逻辑全内联，无 research/replay.py 可复用）。
- `btcore/engine.py:353-448` run() ~95 行单函数：结果读取（395-400）、benchmark_nav 提取（404-415）、stats 落库（418-424）可拆 helper（顺带解决 CONS-02）。

---

## 4. P3 边界小项

| 编号 | 位置 | 问题 | 建议 | 状态 |
|---|---|---|---|---|
| EDGE-01 | `btcore/match/conditions.py:143-159` | handle_stop_loss 在 bar 缺 open/low 键时 `bar_get(..., 0.0)` 回退 0.0 → `0.0 <= stop` 恒真 → 每日"成交价非法顺延"告警；与 take_profit/limit_buy（缺键不触发）不对称 | 缺键/0 价格按不触发处理 | [侦查] |
| EDGE-02 | `btcore/match/manual.py:236-249` | rebalance `target=0` 显式清仓仍走 cap_by_volume，截断后残留持仓且当日不重试，docstring"清仓不要求整手"与实际不符 | 清仓目标绕过 vol cap，或文档明示截断语义 | [侦查] |
| EDGE-03 | `btcore/engine.py:60-67` | `order_volume_ratio > 1` 被允许——cap 可超当日成交量（物理不可能） | 校验 `<= 1` | [侦查] |
| EDGE-04 | `btcore/limits.py:53-58` | 689xxx（科创板 CDR）落 default 10% 规则，未收录 688 同款 | 确认 689 板块规则并入表 | [√] |
| EDGE-05 | `btcore/engine.py:42-73` | config 引擎已知键 typo（slippage_tick / commision_rate / max_position）静默回退默认值；`_validate_select_actions` 有未知键告警先例 | 对与已知键编辑距离小的未知键发 WARNING | [侦查] |
| EDGE-06 | `btcore/match/manual.py:36-37` | "卖出名单含未持仓标的"用 logger.info 而非 _warn → quiet_skips 不生效 | 统一走 _warn | [侦查] |
| EDGE-07 | `btcore/engine.py:325` | `ml_log` 只认字面量 "full"，typo "FULL"/"all" 全部静默当非 full | 校验取值或大小写不敏感 | [侦查] |
| EDGE-08 | `btcore/engine.py:492-494` | 因子物化 issue 经 `getattr(logger, issue["level"])` 动态取日志方法，level 非法字符串时 AttributeError 崩 run | 白名单映射 {warning,error} | [侦查] |
| EDGE-09 | `btcore/strategy.py:68-69` | 程序化 `cls(config=...)` 路径绕过 `_validate_conditions`，未知条件键静默忽略（loader 路径有校验） | Strategy.__init__ 或 ConditionBuilder 加已知键校验 | [侦查] |
| EDGE-10 | `btcore/ml/spec.py:191` | scaler 维度校验仅当 scaler_mean 非空；空 scaler 数组的 meta 通过 → 推理侧静默跳过标准化 | 空 scaler 数组直接报错（除非显式声明无缩放） | [侦查] |
| EDGE-11 | `btcore/generic_sql.py:316-330` | benchmark adj 表部分日期缺失 → merged adj_factor NaN → hfq NaN → pct_change/dropna 静默出洞（仅全空才 warn） | 检测 NaN 占比并 warn/raise | [侦查] |
| EDGE-12 | `btcore/corporate.py:76-88` | cash_div 缺 bar（pre_close=0）时 scale=None：cost 照减但 entry_price/条件锚点不 rescale，corporate_log 记 None 使 apply_condition_rescale 静默跳过 | 缺 bar 时告警一次 | [侦查] |
| EDGE-13 | `btcore/factors/plan.py:118-119` | derive_idx_ret 依赖 trade_date 为 `%Y%m%d` 字符串；backend 返回 Timestamp 则 dates.map(ret) 全 NaN 静默 | 加 dtype 断言或统一归一 | [侦查] |
| EDGE-14 | `factors/library.yaml:376-379` | downside_vol20 的 `roc*(roc<0)` 对 NaN 得 0 而 `min(x,0)` 保留 NaN，注释"替代 min(x,0)"不完全等价（warmup 首行被计为 0 收益） | 掩码内保留 NaN 或注明差异 | [侦查] |
| EDGE-15 | `btcore/ml/labels.py:212-213` | 短回合（<3 交易日）静默 continue，同函数其它残缺场景全部告警 | 补 warning | [侦查] |
| EDGE-16 | `btcore/ml/metrics.py:29-64` vs `research/factor_eval.py` | summarize_ic 键名（ic_pos_ratio/icir vs ic_positive_ratio/ic_ir）与 layered_returns 语义（层均收益 vs 累计序列）不同，ml_guide "同口径"说法过度 | 对齐键名或在 ml_guide 注明差异 | [侦查] |
| EDGE-17 | `btcore/stats.py:91-99` | 回撤未恢复时 dd_recovery_days = len(after_dd)（含谷值日），报告显示"1 天修复"具误导性 | 未恢复时输出 None/0 或改名 days_since_trough | [侦查] |

---

## 5. 测试缺口

| 编号 | 位置 | 问题 | 建议 | 状态 |
|---|---|---|---|---|
| TEST-01 | `pyproject.toml:8-17` + `tests/test_ml.py:557-559` | ML 依赖（onnxruntime/xgboost/onnxmltools/scikit-learn）完全未声明；test_ml 模块级 importorskip 三包 → 全新 `uv sync` 后整个 test_ml.py **54 个用例静默跳过**，ML 全链路零强制覆盖 | dev extra 声明 4 依赖；importorskip 收窄到真正需要 ONNX 的用例 | [√] |
| TEST-02 | `tests/test_ml.py:946-1015` | train_panel/train_guard **从不被任何测试调用**——XGBoost 训练路径（spw、embargo×scaler、best_iteration、metrics）零覆盖；无确定性测试；n_jobs 未 pin（hist 多线程跨核数存在浮点级差异） | 加训练冒烟 + 确定性回归；`n_jobs` 固定或 `deterministic_histogram=True` | [侦查] |
| TEST-03 | `strategies/examples/`（7 个示例） | 6/7 示例零测试覆盖（仅 rolling_ranker 被引用）；示例 config 与 loader 校验漂移无兜底 | 加烟雾测试：遍历 examples/*/config.yaml 全量 load_strategy 通过 | [√] |
| TEST-04 | `tests/conftest.py:36-46` | MockDataBackend 每实例重读 10 个 parquet，每测试 new 一个 backend，全套件重复 IO | session 级 fixture 缓存（数据只读） | [侦查] |
| TEST-05 | `tests/test_ml.py:264,279` | 直连私有符号 `_fit_scaler/_scale` 断言 scaler 语义，改名即碎 | 收敛公开 API 或测试内影子对照 | [侦查] |
| TEST-06 | `tests/test_live.py:268-269` | TestOpSheet 直接实例化 TestBacktestParity 调其私有 `_run_backtest`——跨测试类耦合 + 全量回测 setup 重复 | 提取共享 fixture | [侦查] |
| TEST-07 | `tests/test_debug_snapshot.py::TestReplayDefaultRun` | pytest 全绿但有 3 个 RuntimeWarning（空切片 var：`Degrees of freedom <= 0`） | 空切片时跳过 var 计算或显式处理 | [√实测] |
| TEST-08 | 小项 | test_limits.py:52-55 只断言 up 未断言 down；test_column_pruning.py:117-119 断言 `len(columns) > 10` 过宽；test_factor_universe_e2e.py:28 依赖私有 `backend._bars`；CSE 嵌套提取/neutralize 单行业日/zscore 常量截面日均无用例；export_model 无单测（meta 完整性/verify 失败路径）；labels 窗口外回退分支无用例 | 逐项补回归 | [侦查] |

---

## 6. 文档漂移（全部机械可修）

| 编号 | 位置 | 漂移 | 状态 |
|---|---|---|---|
| DOC-01 | `CLAUDE.md:20,27-33` | "407 测试"（现 539）；engine.py:113/291/404（现 353/555/649）；strategy_loader.py:147、build_strategy :101 均过期 | [√] |
| DOC-02 | `ARCHITECTURE.md:19,23,45` + 测试节 | "88 个因子"（现 126）；"407 测试"、"engine.py 657 行"（现 905）；"test_ml.py 27 个"（现 54） | [√] |
| DOC-03 | `docs/ml_guide.md:77` | "meta 版本不是 2" 应为 3（`spec.py:33 META_VERSION=3`；同文档 §3.3 写 3，自相矛盾） | [√] |
| DOC-04 | `docs/factor_library.md:288-290` | §6.4 ema_bullish 示例仍是 `(a>b)+(c>d)` 布尔直接相加并称"可直接相加"——正是 F-EMA-01 的 OR 化写法，与 library.yaml:258-259 的 `* 1` 修复直接矛盾 | [√] |
| DOC-05 | `docs/cli_and_research.md` 2.2 参数表 | 缺 `--benchmark`（`scripts/factor_eval.py:58` 实际存在，引用 idx_ret 时必需） | [侦查] |
| DOC-06 | `docs/audit_checklist.md:329` | M-SCOPE-01 行号漂移（panel 物化实为 engine.py:498 非 :245；holding 注入 :733-754 非 :521-523） | [侦查] |
| DOC-07 | `scripts/check_skill_sync.py:99-110` | 机械漂移检查只覆盖 .omp/skills/，docs/ 不在覆盖范围（DOC-03/04 因此存活） | [侦查] |
| DOC-08 | `AGENTS.md` | "521 个测试" 已过时（539）；其余契约未漂移 | [√] |

---

## 7. 仓库卫生

| 编号 | 位置 | 问题 | 建议 | 状态 |
|---|---|---|---|---|
| HYG-01 | `adapters/tushare.py.template` | 模板与真实实现严重漂移：模板引用 `daily.open/high/...`，真实实现已切 `stk_factor_pro.*`；模板 67 行 vs 真实 250 行（缺 get_default_db_path 等）。两真实文件均被 .gitignore 排除，**模板是唯一提交版参考**——新机器按模板初始化会得到错误表单 | 模板与真实实现同步（或模板标注"仅骨架，以 tushare.py 为准"） | [√] |
| HYG-02 | `factors/library.yaml` | ~126 因子仅 4 个被策略引用（mom20/low_turnover/bias20/pct_above_ma20）；60+ 因子在 strategies/research/scripts/docs/tests 五处零命中（max_dd_20/calmar_20/sharpe_20/5×_neutral/amihud_illiq/ret_skew20/ret_kurt20/roc_1..20/rsi_z/bb_width/adx_trend/accrual_z/cfo_quality/leverage_change 等） | 加 `status: archived` 标记或移入 library.archive.yaml，提升"在产因子"可发现性（保留定义，勿删——ML 模型 meta 可能引用） | [√grep] |
| HYG-03 | `btcore/factors/ops.py:197-233` | 21 算子中 8 个（delta/ema/min/beta/resid_std/rank/winsorize/group_rank）在 library.yaml 零使用，仅测试+文档覆盖 | 白名单 API 保留合理，各补一个 YAML 示例因子防隐性回归 | [侦查] |
| HYG-04 | `scripts/live_e2e_check.py:28,36-37` | `--days` 默认 `"20260105,20260204"` 已过期；`WINDOW`/`BT_START` 定义后从未使用（死参数） | 删死参数或接回逻辑；默认日期改为相对最近窗口 | [√] |
| HYG-05 | `scripts/ml_train.py:113-117` | provider 构建后不 close（资源泄漏）；文件内 logger 未用 | try/finally close；删未用 logger | [√] |
| HYG-06 | 小项 | `archive/` 未显式列入 .gitignore（现状靠 `*.db` 兜底）；`btcore/factors/library.py:268` `extract_expr_names(text) - set()` 减空集死表达式；`plan.py:375` `queue.pop(0)` O(n²)（~120 因子可忽略）；`database.py:132` write_daily 无列名位置 VALUES（schema 增列即错位）；`database.py:138-155` write_holdings 循环内逐行 `datetime.now()`；`stats.py:47` `format="mixed"` 慢解析（日期保证 YYYYMMDD，改 `%Y%m%d`）；`spec.py:139` "track 键已废弃"死注释 | 逐项微修 | [√/侦查] |

---

## 8. 跨轮遗留（用户决策项，未纳入批次）

1. **数据洞日重同步**：bak_basic 20200803 全表缺行、20191128/20200720/20200804/20210129 部分缺行；600837.SH 无 stock_basic 行；stk_limit 上市首日哨兵 999999.999/0.01（001379.SZ）——重新同步对应表（2026-08-03 核验记录遗留）。
2. **F-OP-04**：`btcore/factors/plan.py:411-417` `_project` 硬编码 industry 组键，其他组键会静默错误——组键参数化。
3. **F-EX-02**：where 两路径 NaN 语义归零（实际闭包暴露面 0 行，可关闭）。
4. **F-BRD-03**：`btcore/factors/library.py:383` compute_breadth 对 group_mean 因子 `first()` 有损量化（223/243 日多行业组，worst 0.198 vs 真实 [-0.027,0.299]）——group_mean 按 (date,industry) 显式聚合口径。（F-BRD-02 伪列附着已修，此条为残留。）

---

## 9. 分批整改路线

每批独立可 PR，按依赖排序；批内项互不阻塞。

### 批次 1 — 口径与正确性（先于一切数字相关改动）
1. CONS-02 统一结果读取器（engine 改调 read_run_data）
2. CONS-04 SELL 超卖告警
3. CONS-03 total_dividend 双口径
4. CONS-05 where 加载期 fail-fast
5. CONS-07 trainer._scale inf 处理
6. CONS-08 ml_train 空交集 fail-fast
7. EDGE-03/05/08（order_volume_ratio、config typo 告警、issue level 白名单）
- 验证：`pytest tests/ -q` + 一次真实回测 + `python scripts/cross_validate.py` 复查

### 批次 2 — 性能热路径
1. PERF-04 exit_conditions 惰性 limits_fn（改动最小收益最大）
2. PERF-02 div_log groupby 查表
3. PERF-01 neutralize 向量化
4. PERF-08 dividend 区间剪枝
5. PERF-06 write_holdings 降频（先确认无外部读方）
6. PERF-07 bench 缓存存 returns
7. PERF-05 limits_fn step 级 memo
- 验证：`pytest tests/test_factor_ops.py tests/test_invariants/` + `bench_universe_preload.py` 前后对比

### 批次 3 — 重复代码收敛（撮合层 helper、ML 共享）
1. DUP-01/02 撮合层 `_warn_skip_reason` / `_cash_affordable`
2. DUP-03 `_validate_buy_conditions` 拆分
3. DUP-06/07 特征行组装 + scaler 共享（顺带 CONS-07 复核）
4. DUP-08 零散（strategy_tools 校验上提、labels 列名常量、replay 薄壳化）
5. DUP-04/05 CSE 增强（数值等价验证）
- 验证：`pytest tests/ -q` + ML 训练/推理对拍

### 批次 4 — 测试与依赖
1. TEST-01 pyproject 声明 ML 依赖 + importorskip 收窄
2. TEST-02 训练冒烟 + 确定性测试（n_jobs 固定）
3. TEST-03 examples 全量 load 烟雾测试
4. TEST-04 conftest session fixture
5. TEST-07 RuntimeWarning 消噪
- 验证：全新 venv `uv sync` 后 `pytest tests/test_ml.py -q` 不再跳过

### 批次 5 — 文档与卫生（机械批量）
1. DOC-01/02/08 数字与行号批量修正（以 ARCHITECTURE.md 为唯一事实源）
2. DOC-03/04 事实修正
3. DOC-07 check_skill_sync 扩展到 docs 关键断言（防止再次漂移）
4. HYG-01 模板同步、HYG-04/05 死参数清理
5. HYG-02 因子 archive 标记（需用户确认哪些是研究性保留）
- 验证：`python scripts/check_skill_sync.py` + `ruff check` 全绿

### 建议不做（保持现状）
- 物化面板 float32 下推（PERF-09）：数值风险大于收益，面板规模未到瓶颈前不做
- 重构 `Engine.run` 大拆（DUP-08 末项）：可拆但收益低，随批次 1 顺带即可
- 因子库"瘦身"删除零引用因子：ML meta 可能引用，且研究循环会复用，标记即可

---

## 10. 验证命令速查

```bash
uv run pytest tests/ -q                          # 全量（553，含新增 14 用例）
uv run ruff check btcore/ tests/ scripts/ research/ strategies/ factors/ adapters/
uv run python scripts/check_anticorrupt.py       # 反破坏 linter
uv run python scripts/check_skill_sync.py        # skill 同步（含 check_docs_facts）
uv run python scripts/run.py strategies/examples/rolling_ranker/config.yaml --start 20240603 --end 20240628
uv run python scripts/bench_universe_preload.py  # 性能前后对比
```

---

## 11. 执行状态（2026-08-06 已实施）

4 片并行实施 + 主线统一验证（证据见文末验证表）。**553 passed（+14）**、ruff / anticorrupt / skill_sync 全绿、fixtures 与真实库（58G）端到端 smoke 通过。

### 已实施（按指导编号）
| 批次 | 项 |
|---|---|
| 1 口径/正确性 | CONS-02/03/04/05/07/08、EDGE-01/02/03/04/05/06/07/08/09/10/11/12/13/15/17 |
| 2 性能 | PERF-01/02/04/05/06/07/08 |
| 3 重复/维护 | DUP-01/02/03/04/05/06/07/08（部分） |
| 4 测试/依赖 | TEST-01/02/03/04/05/06/07/08（部分） |
| 5 文档/卫生 | DOC-01..09、HYG-01/04/05/06、EDGE-14/16（文档侧） |

### 行为变更点（数字会变化的仅标注项）
- **trade_log 排序** `ORDER BY id` → `ORDER BY date, id`（CONS-02 修复，stats/回放事件序统一）
- **holdings 表**：逐日快照 → 仅终态快照（PERF-06，运行期无读方）
- **rebalance target=0 清仓**不再受成交量 cap 截断（EDGE-02）
- **止损缺 open/low** 不再误触发/误告警（EDGE-01）
- **689 板块**涨跌幅 10% → 20%（EDGE-04）
- **order_volume_ratio > 1** 构造期拒绝（EDGE-03）
- **直接构造路径 conditions** 未知键 fail-fast（EDGE-09）
- **where 算子 + 纯 expr 因子** 加载期 fail-fast（CONS-05）
- **trainer ±inf** 填 0（CONS-07，与推理侧对齐；实际路径 build_panel 已归一 NaN，无数字变化）
- **ml_train 训练域空交集** fail-fast（CONS-08）
- **新增 stats 键**：`total_dividend_accrued`、`max_dd_unrecovered`（旧键值不变）
- **新增告警**：SELL 超卖、dividend NaN 占比、cash_div 缺 bar、ML 短回合、config 键 typo（全部仅日志）

### 未做（明确记录）
| 项 | 原因 |
|---|---|
| PERF-09 float32 下推 | 用户确认不做（数值风险 > 收益） |
| 4a 数据洞日重同步 | 用户确认：日行情完整，缺行属 tushare 官方数据缺失，代码软回退不误杀，关闭 |
| HYG-02 因子 archive 标记 | 降级为 docs/factor_library.md §18 零引用清单（86 个）；保留定义勿删 |
| DUP-08b universe 双查注入 | 已实施（filters 模块级 weakref 缓存，2026-08-06 二轮） |
| 跨轮遗留 | F-OP-04/F-EX-02/F-BRD-03 已实施（2026-08-06 二轮）；数据洞日见上 |
| EDGE-16 代码侧键名对齐 | 文档注明差异替代（DOC-09） |

### 二轮执行（2026-08-06，用户拍板后）
| 项 | 状态 | 说明 |
|---|---|---|
| CONS-01 三套 realized pnl 统一 | 已实施 | round_trip 改为含费用口径（买卖净额按比例分摊）；与 symbol_contribution / ML 标签一致。**报告数字会变**（旧口径不含费用）——研究基线需用新代码重跑 |
| F-EX-02 where NaN 语义 | 已实施 | 新增 `expr.where_mask`（NaN/0/False → 掩码），算子与纯表达式两路径统一；此前算子路径 astype(bool) 把 NaN 当 True 保留 |
| F-OP-04 组键参数化 | 已实施 | `_project(group_col="industry")` 参数化 + 缺列 fail-fast |
| F-BRD-03 breadth 组均值 | 已实施 | compute_breadth 对 group_mean 因子按 (date,industry) 组值等权平均（此前 first() 任意取一组）；`test_group_mean_values_match_hand_compute` 期望 20→25 |
| DUP-08b universe 双查 | 已实施 | filters 模块级 weakref 缓存（避免 id 复用串数据），同区间只查一次 backend |

### 验证证据表
| 验证 | 结果 |
|---|---|
| pytest 全量 | 553 passed（+14：test_ml_train 6、test_examples_load 7、参数化新增），零 RuntimeWarning |
| ruff / anticorrupt / skill_sync | 全绿（ruff 修复 2 处 I001 导入排序） |
| 残留符号 grep | scaler 三侧收敛到 ml/scaler.py、engine 内联读取器已删、friction 新签名调用点一致 |
| fixtures smoke | Engine 全窗口：holdings 终态 4 行、run completed、trade_log 有序、set_dividend_bounds 探测不崩 |
| 真实库 smoke | rolling_ranker 20240603-20240628：72 笔交易、33 stats 键、HTML 报告生成（25s） |
| PERF-08 剪枝 | GenericSQLBackend 设窗口后窗口外日期查空、窗口内正常 |
